The general plan goes as follows:


Build docker compose file with:
SQL SERVER (Azure Edge SQL functionally the same)
SQLPAD (To view and query data)
SVELTEKIT (Frontend with API and CSR AND SSR)
ADMINER (General)

